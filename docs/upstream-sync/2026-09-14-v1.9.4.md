# 2026-09-14：合入权威上游 1.9.4

状态：融合中。本轮只记录实际执行结果；上一轮测试和正式环境证据不作为本轮通过依据。

## 目标与权限

| 项目 | 本轮事实 |
|---|---|
| 开发分支、原 VMware SHA/版本 | `vmware`；`119d525cbb22c3a88dccd9057245d2afa44b5573`；`1.9.1-vmware.22` |
| 上次已合上游 SHA、新固定目标 SHA/版本 | `64c1c175261a9c1a68a346377c1108765ec601e4`（`1.9.1`）；`8a989e8f3871c1eaa1e797925e1cac00456e03bd`（`1.9.4`） |
| 本轮 fetch 后的 origin/main、upstream/main SHA 及差异处理 | 两者均为 `8a989e8f3871c1eaa1e797925e1cac00456e03bd`，ahead/behind 为 `0/0`；未接触 `develop` |
| 共同祖先（全部） | `64c1c175261a9c1a68a346377c1108765ec601e4`；上次目标是本次目标祖先 |
| 集成分支和 worktree | `codex/vmware-sync-v1.9.4`；`/home/suyi/src/mdd-sim-gateway-sync-v1.9.4` |
| 修改 / 提交 / 推送 / 部署授权范围 | 已授权开发修改、隔离分支/worktree、普通合并和必要本地提交；未授权推送、正式部署、生产服务重启或生产数据修改 |
| 快照要求、用户确认及恢复点说明 | 本轮不进入正式环境，无快照或恢复点操作 |
| 硬件、SIM 品牌/PLMN、发行版验收范围 | 仅使用虚构数据做 Linux 本地测试；真实设备、卡、运营商和付费接口均不连接 |

`vmware` 与 `origin/vmware` 在 fetch 后均为
`119d525cbb22c3a88dccd9057245d2afa44b5573`，ahead/behind 为 `0/0`。原开发 checkout
已有未提交的 `AGENTS.md` 与 `docs/upstream-sync/TEMPLATE.md` 规则增强；内容逐字带入本集成
worktree，原现场不清理、不 stash、不覆盖。上一轮
`/home/suyi/src/mdd-sim-gateway-sync-v1.9.1` worktree 保持原样且工作区干净。

## 上轮遗留复核

| 来源记录 / 遗留事项 | 本轮重新核实结果 | 本轮处理或继续保留的原因 |
|---|---|---|
| 1.9.1 的普通号码呼入/呼出、双向音频、DTMF、短信送达及全部 codec | 本轮没有真实运营商或音频硬件，不能沿用上轮结果 | 继续标为未验证；本轮只做隔离行为测试与构建身份门禁 |
| Quectel、eSIM、双线路并发、DITO/CTExcel、其他支持发行版及重启/故障回滚 | 测试夹具可覆盖分支逻辑，真实链路仍未提供 | 保留硬件/VM 验收门禁，不用单测替代 |
| 三次 Q.850 127 的历史失败及后来恢复 | 没有新的 SIP 交互证据，根因仍未确认 | 不把本轮代码或只读检查归因为历史恢复原因 |
| 上轮自动化门禁 | 仅作为测试范围线索，未作为本轮证据 | 本轮从零重新运行相关回归、全量、WebUI 和 Engine 构建 |
| “删除指定本地备份”产品缺口 | 代码仍无受管删除入口；它早于 1.9.1 且不属于上次漏合 | 本轮不扩大实现范围，作为独立待办保留 |

## 功能与冲突决策

| 功能 / 路径 | 上游提交依据 | 覆盖状态 | VMware 处理与理由 | 验证结果 / 证据 |
|---|---|---|---|---|
| 每线路 SIP User-Agent：`control/app/config.py`、`engine/render.py`、`webui/src/views/SimConfig.jsx` | `1b8e2cd0008b91de72719a5fa40fa841ac1fef8b` | 完整合入 | 保留默认值；保存与 Engine 二次规整均限制单行可打印 ASCII、移除 `;`、最多 64 字符，防止注入 | 待本轮相关回归与 WebUI 构建 |
| 长短信与 MMS 通知：`control/app/cellular_sms.py`、`control/app/config.py`、`control/app/main.py` | `9ed03b807387e5d8aab6df992412d9adeae8d48a` | 完整合入 | `--` 与 `receiving` 不导入；只有无可读正文且含标准 MIME 标记才清理，三次失败后停止；one-shot 诊断默认只读 | 待本轮相关回归 |
| ICCID 缺失时 IMSI 匹配蜂窝短信/通话：`cellular_sms.py`、`cellular_call.py` | `cbf7da6c9a883293ae75bdcbf6bd86ad57ba71ae` | 完整合入 | 仅 modem ICCID 为空时允许 IMSI；已读到不同 ICCID 立即跳过，不回退 | 待本轮相关回归 |
| AMI 连接失败清理：`control/app/ami.py` | `022caa24ea94ba0459f4da234670e0073a7fd8d7` | 完整合入 | connect 失败/超时关闭 manager，取消其心跳和重连持有 | 待本轮相关回归 |
| Engine MTU 与自动 APN 依赖：`control/app/engine.py`、`install.sh` | `f2cde96e0919ad3df3554afdc91bb0179305e982`、`c9cea431e2108aa3008708d73cfbc53aad703cd6` | 完整合入 | `SWU_TUN_MTU` 进入受管容器；在 VMware 的 APT 安装清单加入 provider 数据库，不引入上游通用安装器 | 待生命周期、安装契约与 Engine 构建 |
| 恢复归因与未知证据：`failover.py`、`main.py`、`engine.py` | `c1d7999e05ee4a07446d1daeb11cd4141b35460d` | 完整合入 | 仅 `tunnel_network` 且 IKE 证据可读时归咎出口；DNS、SIM、认证、协议、Engine 和未知证据不换节点、不清理会话 | 待相关回归 |
| 直连/单节点/未知订阅节点账本：`main.py`、`failover.py` | `c1d7999e05ee4a07446d1daeb11cd4141b35460d`、`e79bffbd28e6b4aedaf84a9418e459fb3df87be7` | 完整合入 | 非可选订阅不建账本；节点暂时未知保留 tried/exhausted/given_up，配置 revision 变化才清空 | 待相关回归 |
| 已运行订阅节点保存免重启：`host/mdd_orchestrator.py` | `c1d7999e05ee4a07446d1daeb11cd4141b35460d` | 完整合入 | 只有 selector 默认值等于运行恢复节点时原子更新配置而不重启；仍执行 VMware 的宿主 DNS 隔离 | 待国家出口回归 |
| ICCID 完整读取：`sim.py`、`pin_keeper.py`、`ami_usim.py`、`swu_ike.py` | `e2f0d0e19684b91c8c0275cdc8338d312d7dc3f8` | 完整合入 | 四条路径均要求 10 BCD 字节、至少 15 位纯数字；不可读只表示不能定罪，继续保留 61/9F、6C、嵌套 EF_DIR | 待 APDU 与超时回归 |
| modem 占位符与桥接元数据：`main.py`、`mdd_orchestrator.py` | `3093b559a592401f7ba421f5179a40edac29116b` | 完整合入 | 过滤 mmcli 占位符；无 IMEI 不丢失桥接 ICCID、槽数和通道元数据 | 待设备状态回归 |
| 蜂窝 profile 路由和关闭安全：`host/mdd_orchestrator.py` | `e2b7cd44adaac01c9486934edfeeef91b09cd15b`、`0838f29b09ae58596647b2b559a8340c8cc43fda` | 完整合入 | profile 永不自行 autoconnect，默认 never-default；更正遗留 profile，按受管名称处理无端口 modem，并在 backend 关闭时有界扫描遗留项 | 待设备状态及安装契约回归 |
| 独立通知投递池：`control/app/notify_push.py` | `c1d7999e05ee4a07446d1daeb11cd4141b35460d` | 完整合入 | Webhook、Telegram、PushPlus 和每个 Feishu 通道独立提交到八线程池，重试不占默认 executor；保留线路过滤与脱敏历史 | 待通知回归 |
| 精确 PLMN 优先：`control/app/carrier_id.py` | `c1d7999e05ee4a07446d1daeb11cd4141b35460d` | 完整合入 | 精确三位 MNC 优先后再应用旧两位兼容；本地 CMLink 仍要求共享 PLMN+SPN | 待 carrier 回归 |
| VoWiFi 历史过期请求：`VowifiHistory.jsx` | `c1d7999e05ee4a07446d1daeb11cd4141b35460d` | 已有等价实现 | VMware 的 `instanceId:range` view key 与递增 request 序号已同时防止换线、换范围、重叠刷新和卸载后的旧结果覆盖，保留时间范围及 portal tooltip | 待更新后的 Node/浏览器回归 |
| ML307X/DITO 共享选择、槽过滤与绑定：`main.py`、Engine 三路径 | `a603b54dc8e8302bddad3b70717e97583d523231`、`908a9e6cf998e7a25d7ec73e601ceda09d65807a` | 已有等价实现 | 使用 VMware 更严格的共享 USIM 选择器；将上游 `channel_allocated` 再作为容量上限，保留现有 logical_channels 过滤、单次完整读取及兄弟 ICCID 快速核对 | 待 eSIM、DITO 与 Engine reader 回归 |
| 无实时 IMEI 时沿用线路旧值 | `a603b54dc8e8302bddad3b70717e97583d523231` | 部分合入 | 合入无 IMEI 时保留有效桥接元数据和槽分配；不直接沿用线路快照。桥接进程可保留同一已打开设备先前验证的 IMEI；无当前物理证明时以 409 停止 | 需新增/调整物理身份保护回归；真实模块未验证 |
| 连接历史过期请求保护 | `c1d7999e05ee4a07446d1daeb11cd4141b35460d` | 已有等价实现 | 与 VMware 的时间范围请求代次收敛，不保留重复状态机 | 待 Node 与三尺寸回归 |
| 网页自动更新、Release 选择、Docker Control、Actions/预编译资产、ARM 限制 | 1.9.2–1.9.4 发布支线中的相关历史内容 | 明确不适用 | `update-policy.json` 和新增 `.github/release-notes` 继续删除；不恢复运行入口、workflow 或 ARM-only 取舍 | 待产品边界搜索与安装契约 |
| 删除指定本地备份 | 早于本次目标的独立缺口 | 确认遗漏 | 不是 1.9.1 漏合，也不在本轮授权功能范围内；不顺带增加删除 API | 后续单独设计 owner-only 受管删除、确认与审计验收 |
| 真实硬件/运营商/通话/音频验收 | 不对应单一提交 | 暂缓 | 本轮禁止连接生产线路，且未提供隔离实机矩阵 | 后续在支持 VM、虚构/自有合法测试卡和明确维护窗口验收 |

## 未完整覆盖的功能

| 功能 / 来源 | 缺少的部分及影响 | 未合入原因 / 决策依据 | 后续处理与验收条件 |
|---|---|---|---|
| 上游旧线路 IMEI fallback | 没有当前硬件证明时不会自动启动该 VoWiFi 线路 | 线路快照不能证明当前 USB 位置仍是同一模块；防止把设备身份借给换入硬件 | 提供受验证的稳定硬件身份或由同一 bridge 生命周期保留已验证 IMEI，并完成真实 ML307X 拔插/换口/换机验收 |
| 删除指定本地备份 | 用户仍不能从受管列表删除某个归档 | 早期独立产品缺口，本轮不扩大数据删除面 | 单独需求需覆盖 owner、普通文件、同名摘要、路径边界、二次确认和失败原子性 |
| 真实业务验收 | 自动测试不能证明 USB、SIM、IMS、通话和音频 | 本轮权限明确排除生产连接与重启 | 后续分别记录 USB/PCSC/ATR、Quectel bearer、出口、SWu、IMS、普通号码呼入呼出、双向音频、DTMF 和短信送达 |

## 冲突与自动合入审核

- 普通合并初始出现 13 个内容冲突和 1 个修改/删除冲突；逐功能处理，未对整文件或整树使用
  `ours/theirs`。
- `main.py` 同时保留 exit ledger 的配置 revision、上游 reason code 归因、VMware eSIM 单次完整读
  及 logical-channel 过滤；旧线路 IMEI fallback 按物理身份边界重写。
- Engine 三路径继续调用 `pin_keeper.select_adf_usim`，没有重新引入各自漂移的选择器；只叠加
  ICCID 完整 BCD 校验。
- `mdd_orchestrator.py` 把蜂窝 profile policy 与 VMware 的飞行模式、输入唤醒、Fake-IP/Xray、
  TUN DNS 隔离和设备迁移合并；selector 免重启路径仍维护 DNS 隔离。
- `install.sh` 没有引入上游多发行版/旧 Docker Control 安装器，只在 VMware APT 清单补充 APN 数据库。
- 自动合入的配置、短信、通知、API、测试和 WebUI 将以相关回归、全量测试和双向差异继续审核。

## 自有功能保留

AI 实时字幕、全局软电话与跨页音频、设备自定义名称、连接历史时间范围、备份迁移、CMLink
home-local 语音短号、Fake-IP 处理、严格 USIM、eSIM 互斥/恢复、Feishu、多线路上限、TCP/UDP
端口探测、Created 容器清理、原生 Control/WebUI、rootful Engine 和受管 `mddctl` 更新边界均保留。

## 测试与构建证据

| 门禁 | 命令/环境 | 结果、跳过原因及证据位置 |
|---|---|---|
| 相关单测、Linux 全量 | 待执行 | 未验证；不沿用 1.9.1 结果 |
| 静态检查、提交范围隐私检查 | 已对已解决核心文件执行 `py_compile`、`bash -n install.sh`、Node 语法检查 | 初步通过；最终全量待执行 |
| WebUI 锁文件构建及三尺寸浏览器回归 | 待执行 | 未验证 |
| 旧数据迁移、重复迁移、恢复副本 | 待执行隔离测试 | 未验证；不读取生产数据库 |
| 精确候选 SHA 的 Engine 构建及产物验收 | 待完成本地提交后执行 amd64 `--no-cache` | 未验证 |

## 交付与正式更新

- 普通合并提交及父提交：待本地提交后补记，不改写历史。
- 后续融合修复提交：待相关回归和精确身份构建结果补记。
- `origin/vmware` 仍为 `119d525cbb22c3a88dccd9057245d2afa44b5573`；本轮不推送。
- 推送、正式更新、服务重启、生产数据备份/恢复：未执行，等待后续明确授权。

## 未验证与遗留问题

真实 SCR Prime、Quectel/ML307X、eSIM profile、各运营商 PLMN、SWu/IMS、普通号码呼入呼出、
双向音频、DTMF、短信送达、MMS 模块存储清理、全部 codec、并发、宿主/客户机重启、四个支持
发行版和真实更新回滚均未验证。自动测试和本地容器构建不得替代这些层级。

## 给下一轮的提示

继续重点复核 `main.py`、Engine 三条 UICC 路径、`host/mdd_orchestrator.py` 和通知异步调用者。
若上游以后提供稳定、可验证的物理 modem 身份，可重新评估无实时 IMEI 的启动策略；在此之前
不要用线路快照或 USB 文件名替代硬件证明。公开记录只保留脱敏结论。
